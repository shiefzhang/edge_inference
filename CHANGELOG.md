# 更新日志

## v0.5.0 - 2026-05-23

### 新增

- 模型管理拆分为 PT 文件管理和模型逻辑管理。
- PT 文件管理支持上传、列表展示、详情查看、标签 ID/名称展示、加载状态和显存占用展示。
- 程序启动时自动预加载 `models/` 目录下的 PT 文件并执行 warmup。
- 模型逻辑支持使用 `app/func/model_*.py` 中的 Python 逻辑，并通过 `model_bindings` 绑定 PT 文件。
- 通道模型选择增加“无模型”选项，可直接预览原始视频。
- 视频监控画面增加实时 FPS 浮层。
- 视频监控指标增加显存占用；在 Jetson 统一内存场景下支持共享内存回退显示。
- 连接测试日志增加分阶段耗时，便于判断慢在前端、后端、ffprobe 还是状态写入。
- 前端增加视频布局日志上报，用于定位浏览器预览缩放问题。

### 优化

- RTSP 连接测试优先使用快速 ffprobe 参数，减少已联通摄像头的测试等待时间。
- 通道运行时对网络流设置 OpenCV FFmpeg 快速打开参数，降低 RTSP 打开和探测耗时。
- 通道状态轮询限制在视频监控页面，登录页和其他页面不再持续轮询 `/api/snapshot`。
- 浏览器预览针对纵向摄像头使用等比例包含显示，避免画面被裁切。
- 启动和停止通道后刷新显存指标。
- 停止通道时清理 FPS、帧数和浏览器预览连接，重新启动时使用新的 MJPEG URL，避免旧画面锁住。
- 推理模型中的标准输出和标准错误统一写入服务日志。
- 日志使用轮转文件，默认写入 `data/server.log`，避免日志无限增长。
- 降低模型逻辑中无检测结果的日志级别，减少 INFO 刷屏。
- 设置页面将 `Author` 文案改为 `作者`。

### 修复

- 修复部分情况下停止按钮被禁用但通道仍在继续显示的问题，增加 worker 阶段日志和推理中断检查。
- 修复停止后再次启动可能继续显示旧 MJPEG 连接画面的问题。
- 修复 PT 详情按钮在部分情况下无法判断请求是否到达后端的问题，增加后端与前端诊断日志。
- 修复未安装 `shapely` 时函数逻辑加载失败的问题，并将依赖写入 `requirements.txt`。
- 修复 uvicorn access log 持续打印 `/api/snapshot` 的问题，默认关闭 access log。

### 维护

- `.gitignore` 忽略 `models/*.pt`，PT 权重作为部署产物不提交到 GitHub。

## v0.4.0

- 视频监控卡片的浏览器预览区域改为固定比例和固定高度范围，不再随摄像头原始分辨率撑开或压缩布局。
- RTSP 输出继续保持摄像头原始分辨率推流，不受浏览器预览尺寸影响。
- 新增通道扩展能力，管理员可在视频监控页面动态增加通道数量。
- 连接配置的默认通道支持选择新增后的通道编号。

## v0.3.0

- 新增历史日志页面，记录登录、用户管理、连接管理、模型函数管理、视频通道操作等关键事件。
- 连接测试增加超时处理，摄像头关闭或 RTSP 不通时会在几秒内返回失败提示。
- RTSP 配置拆分为推流地址和对外播放地址，支持 `RTSP_PUBLIC_HOST`。

## v0.2.0

- 模型管理升级为模型函数管理，不再只管理 `.pt` 文件。
- 支持模型函数增删改查。
- 支持上传替换 `.pt` 权重文件。
- 支持上传替换 Python 推理逻辑代码。
- 支持安全帽、反光衣等多阶段推理流程封装。

## v0.1.0

- 初始化 FastAPI 视频推理管控平台。
- 支持 4 路视频通道并行管理。
- 支持连接列表、用户管理、浏览器预览和 RTSP 输出。
- 内置默认 YOLO 检测/分类模型配置。
